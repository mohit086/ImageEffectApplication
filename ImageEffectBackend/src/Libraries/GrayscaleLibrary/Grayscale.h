#ifndef GRAYSCALE_H
#define GRAYSCALE_H
#include "../Pixel.h"
#include <vector>
using namespace std;
//adding the function declaration in the .h file 
void applyGrayscale(vector<vector<Pixel>> &image);
#endif